# Modelos

Artefatos treinados e registros de modelos ficam na distribuição completa. Treinamento, registro e retreino estão implementados em `src/modeling.py`, `src/model_registry.py` e `src/retraining.py`.
